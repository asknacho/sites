# Project Instructions

## Git Workflow

- **Commit and push directly to main** for all changes. Do not create feature branches or PRs unless there is a security concern (e.g. secrets, credentials, destructive migrations).
